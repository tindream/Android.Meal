package tinn.meal.ping.enums;

public enum LoadType {
    none,

    setText,
    setImage,
    setImageId,
    setAdapter,
    setLine,

    load,
    complete,
    connect,
    connected,
    progress,

    time,
    timeStart,
    timeComplete,

    home,
    report,
    order,
    my,
    notified,
    notifiedUpdate,

    cancel,
    confirm,

    check,
    sync,
    share,
    send,
    received,

    Error,
    Login,
    AutoLogin,
}
