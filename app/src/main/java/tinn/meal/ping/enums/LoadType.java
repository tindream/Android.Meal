package tinn.meal.ping.enums;

public enum LoadType {
    none,
    error,

    setText,
    setImage,
    setImageId,
    setIcon,
    setVisibility,
    setEnable,
    setAdapter,

    local,
    load,
    complete,
    connect,
    data,
    image,
    progress,
    time,
    timeStart,
    timeComplete,

    home,
    report,
    order,
    my,
    notified,
    notifiedUpdate,

    battery,
    confirm,

    wait,
    login,
    loginAuto,
    checkVersion,
    sync,
    share,
    send,
    received,
}
