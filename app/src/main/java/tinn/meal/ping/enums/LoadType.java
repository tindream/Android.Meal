package tinn.meal.ping.enums;

public enum LoadType {
    none,

    setText,
    setImage,
    setImageId,
    setIcon,
    setVisibility,
    setEnable,
    setLine,
    setAdapter,

    local,
    load,
    complete,
    connect,
    connected,
    data,
    image,
    progress,
    time,
    timeStart,
    timeComplete,

    home,
    report,
    order,
    my,
    notified,
    notifiedUpdate,

    confirm,

    wait,
    checkVersion,
    sync,
    share,
    send,
    received,

    Error,
    Login,
    AutoLogin,
}
